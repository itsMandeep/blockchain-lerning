{
  "dependencies": {
    "dotenv": "^16.0.2",
    "ethers": "^5.7.0",
    "fs-extra": "^10.1.0",
    "prettier": "^2.7.1",
    "solc": "^0.8.16"
  },
  "scripts": {
    "compile": "yarn solcjs --bin --abi --include-path node_modules/ --base-path . -o . SimpleStorage.sol"
  }
}
